// InteractiveBackground is intentionally a no-op for the clean Medium-style aesthetic.
// The blog uses a pure white background without canvas effects.
const InteractiveBackground = () => null;
export default InteractiveBackground;
